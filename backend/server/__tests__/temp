
    leaveRooms,
    saveDice,
    validateHighestDice,









    //raceMiniGame,
    //validateRaceMiniGame,
    //validateWinnerRaceMiniGame,


    stockMiniGame, --> done
    auctionMiniGame, --> done
    validateWinner, --> done
    playerCollision, --> done
    playerGetMoney, --> done
    playerLoseMoney, --> done
    navObj, --> done
    updatePlayerPosition, --> done
        validateNextTurn, --> done
        updateDice, --> done
         updateStock, --> done
         resetPlayersDice, --> done
         increaseReadyCounterForRoom, --> done
         stocks, --> done
         createPlayer, --> done
         createRoom, --> done
         searchEmptyRooms, --> done
         validateRoom, --> done
             joinRoom, --> done